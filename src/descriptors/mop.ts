import { ObjectDescriptor } from '../types/objectDescriptor'

export const mop: ObjectDescriptor = {
	texture: 'mop',
	isInteractive: true,
	isStatic: true,
}

// export const dirt = {
// 	texture: 'dirt',
// 	tags: ['clearable']
// }
